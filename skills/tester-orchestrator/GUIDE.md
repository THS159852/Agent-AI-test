# Tester Orchestrator — Guide

Single QA agent. Copy `agents/tester-orchestrator.md` to `.cursor/agents/` and `skills/` to `.cursor/skills/`.

Invoke: `Use tester-orchestrator to ...`

The agent routes to skills — it does not spawn sub-agents.
