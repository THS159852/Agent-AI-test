# Tester Orchestrator — Hướng dẫn

Một agent QA duy nhất. Copy `agents/tester-orchestrator.md` → `.cursor/agents/` và `skills/` → `.cursor/skills/`.

Gọi: `Use tester-orchestrator to ...`

Agent route sang skill — không tạo sub-agent.
