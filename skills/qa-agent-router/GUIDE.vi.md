# QA Agent Router — Hướng dẫn

Một agent QA duy nhất. Copy `agents/qa-agent-router.md` → `.cursor/agents/` và `skills/` → `.cursor/skills/`.

Gọi: `Use qa-agent-router to ...`

Agent route sang skill — không tạo sub-agent.
