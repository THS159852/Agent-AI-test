# QA Agent Router — Guide

Single QA agent. Copy `agents/qa-agent-router.md` to `.cursor/agents/` and `skills/` to `.cursor/skills/`.

Invoke: `Use qa-agent-router to ...`

The agent routes to skills — it does not spawn sub-agents.
